import React from 'react';
import Button from '../Button';
const CategoriesProject = () => {
  return (
    <div>
      <Button text="Semua" color="primary" />
    </div>
  );
};

export default CategoriesProject;
